apiVersion: v2
name: guitar-detect
description: Real-time guitar brand/model detection on a K3s home cluster.
type: application

# Chart-internal version (Helm uses this).
version: 0.1.0
# Application version this chart deploys; SHOULD match image.tag for sanity.
appVersion: "0.1.0"

kubeVersion: ">=1.28.0"
